export const metadata = { title:"stock-framework", description:"Barry Dodd collaborative stock screening and portfolio analysis framework" };
export default function RootLayout({ children }) {
  return (<html lang="en"><body style={{ margin:0 }}>{children}</body></html>);
}
